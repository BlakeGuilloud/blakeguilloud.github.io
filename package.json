{
  "name": "blakeguilloud.github.io",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://blakeguilloud.github.io/",
  "dependencies": {
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10",
    "smoothscroll": "^0.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "pages": "yarn build && gh-pages -d build",
    "deploy": "yarn build && av1 deploy reactApp blakeguilloud default && gh-pages -d build"
  },
  "devDependencies": {
    "av1-scripts": "^1.0.6",
    "gh-pages": "^1.0.0"
  }
}
